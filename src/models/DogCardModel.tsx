export type DogCardModel = {
  imgUrl?: string,
  finename?: string
  label?: string;
  font?: { value: string, label: string };
  color?: { value: string, label: string };
  lastupdate?: string;
  active?: boolean;
}